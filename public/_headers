# Cloudflare (Workers static assets / Pages) header rules.
# Everything under /_astro/ is content-hashed — JS, CSS, fonts and the
# optimized image renditions — so it can be cached forever.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
